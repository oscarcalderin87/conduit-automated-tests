class User {
    constructor(_username, _email, _password, _token) {
        this.username = _username
        this.email = _email
        this.password = _password
        this.token = _token
    }
}

module.exports = User
